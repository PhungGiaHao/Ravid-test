{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "baseUrl": ".",         // trỏ root project
    "paths": {
      "@/*": ["src/*"]      // alias @ tới thư mục src
    }
  }
}
